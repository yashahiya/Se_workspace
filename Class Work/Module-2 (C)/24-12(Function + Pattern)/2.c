#include<stdio.h>
#include<conio.h>
#include<math.h>

int main()
{
    float a = 14.20;
    float b = 2.00;

    printf("\n %f",floor(a));
    printf("\n %f",ceil(a));
    printf("\n %f",sqrt(b));
    printf("\n %f",pow(b,5));
    printf("\n %f",abs(a));

    return 0;
}